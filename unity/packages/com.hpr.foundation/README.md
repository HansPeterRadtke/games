# HPR Foundation

Shared Unity package for common code used across multiple projects.

Current layout:
- `Runtime/Utils` shared runtime helpers
- `Editor` editor-only helpers
- `Tests/Runtime` runtime package tests
